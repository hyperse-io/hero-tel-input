---
title: API Reference
description: Learn about the props and methods available in Hyperse Hero Tel Input.
---

import { generateDefinition, TSDoc } from 'nextra/tsdoc';

# API Reference

This article discusses the API and props of `HeroTelInput`. Props are defined within `HeroTelInputProps`.

<TSDoc
  definition={generateDefinition({
    code: `
import type { HeroTelInputProps ,BaseInputProps } from '@hyperse/hero-tel-input/index.d.ts'
type MyProps = Omit<HeroTelInputProps,keyof BaseInputProps>
export default MyProps
`,
  })}
/>

## Custom classNames

<TSDoc
  definition={generateDefinition({
    code: `
import { Input, type InputProps } from '@heroui/react';
import type { FlagAutocompleteClassNames } from '@hyperse/hero-tel-input/index.d.ts'
type MyProps = FlagAutocompleteClassNames & {
  input?: InputProps['classNames'];
};
export default MyProps;
`,
  })}
/>
