---
title: Getting Started
description:
  Get started with Hyperse Hero Tel Input and start building your next
  international telephone input form.
---

# Getting Started

<Callout type="warning">
  Hero Tel Input depends on [Tailwind CSS](https://tailwindcss.com/) Please
  ensure Tailwind CSS is set up in your project before using Hero Tel Input.
</Callout>

- When using Tailwind v4, add them to your CSS:

```css
@import 'tailwindcss';
@plugin "tailwindcss-react-aria-components";
@plugin "tailwindcss-animate";
```

- When using Tailwind v3, add the plugins to your tailwind.config.js instead:

```js
module.exports = {
  // ...
  plugins: [
    require('tailwindcss-react-aria-components'),
    require('tailwindcss-animate'),
  ],
};
```

## Installation

```sh npm2yarn
npm install @hyperse/hero-tel-input
```

## Usage

```tsx
import { HeroTelInput } from '@hyperse/hero-tel-input';

const MyComponent = () => {
  const [phone, setPhone] = React.useState('');

  const handleChange = (newPhone: string, info: HeroTelInputInfo) => {
    setPhone(newPhone);
  };

  return <HeroTelInput value={phone} onChange={handleChange} />;
};
```

## Example

<iframe
  style={{
    width: '100%',
    minHeight: '500px',
    borderRadius: '8px',
  }}
  src={
    'https://stackblitz.com/fork/github/hyperse-io/hero-tel-input/tree/main/examples/example-nextjs?title=hps-hero-tel-input-example&startScript=serve&embed=1&theme=dark'
  }
/>

## Congratulations!

That's all, now let's deep dive into the [props](/docs/api).
